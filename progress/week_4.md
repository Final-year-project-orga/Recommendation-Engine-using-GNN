**Shubhadip**- Made the inference folder. Made two files, model-loader which taken the trained embeddings and make an object out of it and passes to reccomender.py which checks the validity of the embeddings

**Swarnava**- Meta data filtering and the ranking algorithm is implemented

**Saptarshi**- Implemented LightGCN class and written a function of generating the triplets and written the BPR loss function 

**Retovash:**-------------